#include <cstdlib>
#include <iostream>

using namespace std;

int main()
{
    int addnum(int,int);
    int suma,b;
    cut<<"\n enter two number to be summed:';
    cin>>a>>b;
    sum=addnum(a,b);
    cout<<"\n the sum of "<<a<<"and"<<b<<"is"<<sum;
    return 0;
}
int addnum(int numl,int num2)
{
    int tot;
    tot=num1+num2;
    return(tot);
    return 0;
}
 
